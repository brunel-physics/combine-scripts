Repo for combine scripts and datacards used by the tZq analysis

Requires combine tool, CombineHarvester, and rename from util-linux

Run `combine.sh` to perform fit and create plots. Run `cobmine.sh -h` for usage.

Once combine has been run for 2016 and 2017 use `final.sh` to cobmine results.
